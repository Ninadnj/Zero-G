# 👤 User Preferences (The "Nina" Profile)

> **Preferences for Nina DNJ**

---

## 🎨 Design & UI
- **Aesthetic**: Premium, minimalist, "Quiet Luxury".
- **CSS Engine**: **Tailwind CSS v4** (Always prefer).
- **Icons**: Lucide React or Heroicons.
- **Colors**: High contrast, subtle grays, neon accents (if cyber) or muted refinement (if corporate).

## 🛠️ Tech Stack Defaults
- **Frontend**: Next.js (App Router), React, TypeScript.
- **Backend**: Node.js or Python (FastAPI).
- **Package Manager**: `npm` (default) or `pnpm` (if speed needed).
- **Formatting**: Prettier default settings.

## 📝 Communication Style
- **Tone**: Professional, concise, architectural.
- **Reporting**: "Systems Thinking" – Explain *why*, not just *what*.
- **Task View**: Prefer clear checklists and step-by-step plans.

## 🚫 Anti-Patterns (Don't do this)
- Do **not** use `create-react-app` (Legacy).
- Do **not** use `var` (Use `const`/`let`).
- Do **not** write extensive comments for obvious code.
